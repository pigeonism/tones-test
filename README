This is a small piece of code that uses the functionality of siggen/tones program available for Linux to generate tones by taking in characters the user enters as the basis for which tones
to play. All I have done is just use character input to map against tone values in Python, for use with a program that already exists

Uses siggen/tones and aoss on linux
